"""
This module generates a file that contains the hyperparameters for training different NNs.
The advantage of this strategy
to handle an experiment campaign is that, in case of partial failure of Windows, it is possible to resume the experiment
where it is interrupted
"""

FILE_NAME = 'exp_hyperparams.txt'
